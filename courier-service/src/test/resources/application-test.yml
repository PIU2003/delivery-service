spring:
  data:
    mongodb:
      database: courierdb-test
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.7

app:
  api-key: test-courier-key
  api-key-header: X-API-KEY
