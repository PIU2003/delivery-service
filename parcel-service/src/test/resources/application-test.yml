spring:
  data:
    mongodb:
      database: parceldb-test
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.7

app:
  api-key: test-parcel-key
  api-key-header: X-API-KEY
